#!/bin/sh

. "${SCRIPTS_DIR:-$HOME/scripts}/scriptsrc"
. "$lib/csv"
. "$lib/clipboard"

emoji_key=$(keys "$lib/data/emojis" "" "!/^ *#/ && NF" | menu 3 "Emojis")

if [ -n "$emoji_key" ]
then
    emoji=$(echo "$emoji_key" | get_column " " "1")

    set_clipboard "$emoji"

    notify-send "Copied" "$emoji"
fi

