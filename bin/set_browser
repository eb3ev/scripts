#!/bin/sh

. "${SCRIPTS_DIR:-$HOME/scripts}/scriptsrc"
. "$lib/var/config"
. "$lib/browser"

new_browser=$(installed_browsers | menu 2 "Browsers")

if [ -n "$new_browser" ]
then
    set_value "$new_browser" "$browserrc"
fi

