#!/bin/sh

. "${SCRIPTS_RC:-$HOME/scripts}/scriptsrc"

case "$1" in
    0) echo "$2" | $lib/encode/enc_awk_regex.sed ;;
    1) echo "$2" | $lib/encode/enc_sed_regex.sed ;;
    2) echo "$2" | $lib/encode/enc_html.sed ;;
esac
